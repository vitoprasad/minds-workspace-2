# Every active (non-comment, non-blank) pattern in this file must either
# start with `**/` or contain a path separator before its last character.
# This is enforced by `test_gitignore_patterns_use_double_star` in
# test_meta_ratchets.py so that .dockerignore (which is a symlink to this
# file -- Docker reads the target) honors the same exclusions as git.
# Bare patterns like `runtime/` are gitignore-recursive but match only at
# the build context root under dockerignore syntax; the `**/` prefix
# makes both formats agree.

# Workspace data. Everything under data/ is runtime-written user/workspace
# data (memories, tickets, uploads, per-creation data, machine state,
# secrets): gitignored, never pushed to GitHub, covered by the restic
# host-backup service. The README.md negations keep only the folder-guide
# files tracked -- and, via the .dockerignore symlink, in the image -- so the
# directory skeleton and its documentation ship without any data ever being
# committed. The laddered ignore/negate style is required by gitignore
# semantics: a file cannot be re-included when its parent directory is
# excluded, so each level re-includes the directory before re-ignoring its
# contents.
data/*
!data/README.md
!data/documents/
data/documents/*
!data/documents/README.md
!data/my-project/
data/my-project/*
!data/my-project/README.md
!data/memories/
data/memories/*
!data/memories/README.md
!data/uploads/
data/uploads/*
!data/uploads/README.md
!data/system/
data/system/*
!data/system/README.md
!data/.apps/
data/.apps/*
!data/.apps/README.md
!data/.skills/
data/.skills/*
!data/.skills/README.md
!data/.state/
data/.state/*
!data/.state/README.md
!data/.secrets/
data/.secrets/*
!data/.secrets/README.md
!data/.tasks/
data/.tasks/*
!data/.tasks/README.md
!data/.imbue/
data/.imbue/*
!data/.imbue/plans/
data/.imbue/plans/*
!data/.imbue/plans/CLAUDE.md

# Sculptor local config (per-checkout, not shared).
**/.sculptor/

# Login-modal reshape mocks (design exploration, not shipped).
docs/system/specs/login-modal-reshape/

# Playwright MCP session artifacts.
**/.playwright-mcp/

# tk tickets (tracker is vendored at system/vendor/tk; the tickets themselves are local state)
**/.tickets/
# ...except the folder guide for the workspace's own tickets dir (this must
# come after the **/.tickets/ rule so the negation wins).
!data/.tickets/
data/.tickets/*
!data/.tickets/README.md

# External repos we're actively working on (e.g. mngr) live here as standalone
# clones, separate from the read-only vendor/ subtrees. See CLAUDE.md.
**/.external_worktrees/

# Byte-compiled / optimized / DLL files
**/__pycache__/
**/*.py[codz]
**/*$py.class

# C extensions
**/*.so

# Distribution / packaging
**/.Python
**/build/
**/develop-eggs/
**/dist/
**/downloads/
**/eggs/
**/.eggs/
**/lib/
**/lib64/
**/parts/
**/sdist/
**/var/
**/wheels/
share/python-wheels/
**/*.egg-info/
**/.installed.cfg
**/*.egg
**/MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
**/*.manifest
**/*.spec

# Installer logs
**/pip-log.txt
**/pip-delete-this-directory.txt

# Unit test / coverage reports
**/htmlcov/
**/.tox/
**/.nox/
**/.coverage
**/.coverage.*
**/.cache
**/nosetests.xml
**/coverage.xml
**/*.cover
**/*.py.cover
**/.hypothesis/
**/.pytest_cache/
**/cover/

# Translations
**/*.mo
**/*.pot

# Django stuff:
**/*.log
**/local_settings.py
**/db.sqlite3
**/db.sqlite3-journal

# Flask stuff:
**/instance/
**/.webassets-cache

# Scrapy stuff:
**/.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
**/.pybuilder/
**/target/

# Jupyter Notebook
**/.ipynb_checkpoints

# IPython
**/profile_default/
**/ipython_config.py

# pyenv
#   For a library or package, you might want to ignore these files since the code is
#   intended to run in multiple environments; otherwise, check them in:
# .python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   install all needed dependencies.
#Pipfile.lock

# UV
#   Similar to Pipfile.lock, it is generally recommended to include uv.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#uv.lock

# poetry
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
#poetry.lock
#poetry.toml

# pdm
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#   pdm recommends including project-wide configuration in pdm.toml, but excluding .pdm-python.
#   https://pdm-project.org/en/latest/usage/project/#working-with-version-control
#pdm.lock
#pdm.toml
**/.pdm-python
**/.pdm-build/

# pixi
#   Similar to Pipfile.lock, it is generally recommended to include pixi.lock in version control.
#pixi.lock
#   Pixi creates a virtual environment in the .pixi directory, just like venv module creates one
#   in the .venv directory. It is recommended not to include this directory in version control.
**/.pixi

# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
**/__pypackages__/

# Celery stuff
**/celerybeat-schedule
**/celerybeat.pid

# SageMath parsed files
**/*.sage.py

# Environments
**/.env
**/.envrc
**/.venv
**/.venv-*
**/env/
**/venv/
**/ENV/
**/env.bak/
**/venv.bak/

# Spyder project settings
**/.spyderproject
**/.spyproject

# Rope project settings
**/.ropeproject

# mkdocs documentation
/site

# mypy
**/.mypy_cache/
**/.dmypy.json
**/dmypy.json

# Pyre type checker
**/.pyre/

# pytype static type analyzer
**/.pytype/

# Cython debug symbols
**/cython_debug/

# PyCharm
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
**/.idea/

# Abstra
# Abstra is an AI-powered process automation framework.
# Ignore directories containing user credentials, local state, and settings.
# Learn more at https://abstra.io/docs
**/.abstra/

# Visual Studio Code
#  Visual Studio Code specific template is maintained in a separate VisualStudioCode.gitignore
#  that can be found at https://github.com/github/gitignore/blob/main/Global/VisualStudioCode.gitignore
#  and can be added to the global gitignore or merged into this file. However, if you prefer,
#  you could uncomment the following to ignore the entire vscode folder
# .vscode/

# Ruff stuff:
**/.ruff_cache/

# PyPI configuration file
**/.pypirc

# Cursor
#  Cursor is an AI-powered code editor. `.cursorignore` specifies files/directories to
#  exclude from AI features like autocomplete and code analysis. Recommended for sensitive data
#  refer to https://docs.cursor.com/context/ignore-files
**/.cursorignore
**/.cursorindexingignore

# Marimo
marimo/_static/
marimo/_lsp/
**/__marimo__/

# task folders for local custom claude workflow
*/*/_tasks/
**/.sesskey

# Node.js
**/node_modules/

# Frontend build artifacts
**/frontend-dist/

# Frontend build artifacts and binary assets of the shell and the chat app
# (built fresh on mind creation via `cd system && npm ci && npm run build`)
system/apps/system_interface/imbue/system_interface/static/
system/apps/chat/imbue/chat/static/
system/apps/system_interface/frontend/media/*.ico
system/apps/chat/frontend/media/*.ico
system/apps/system_interface/frontend/src/assets/*.png

# Claude Code local settings (session-specific permissions)
.claude/settings.local.json

# Ephemeral git worktrees created for isolated subagent runs
.claude/worktrees/

# PR status and URL files written by main_claude_stop_hook.sh for status line display
.claude/pr_url
.claude/pr_status

# Local Claude settings backup files
.claude/*.bak

# ScheduleWakeup lockfile written by Claude Code at runtime
.claude/scheduled_tasks.lock

# Local mngr settings (not committed)
.mngr/settings.local.toml

# so the user can make their own notification script
system/scripts/notify_user.local.sh

# Test output files (slow tests, coverage summaries)
**/.test_output/

# TMR output directories (reports + test outputs)
**/tmr_*/

# Active session marker file (used to detect interrupted sessions)
.claude/active

# we stick the image build artifacts here
.mngr/dev/build/
.mngr/dev/secrets/

# Offload caches and local files
.offload/**
**/test-results
**/current.tar.gz

# Mind deploy-time build artifacts
**/.minds/

# Autofix session artifacts
**/.autofix/
.reviewer/outputs/
.reviewer/settings.local.json
**/.reviewer/.stop_hook_consecutive_blocks
**/.reviews/

# Stuck agent tracking (stop hook escape valve)
.claude/blocked_stop_commits

# Demo recordings (asciinema .cast, .txt, and scripts)
**/.demos/

# ignore coverage files generated within subprojects
**/cov.xml

# ignore demos
**/recordings

# make sure we don't check in reviewer logs
.reviewer/logs/

# Reviewer plugin scratch output
.reviewer/tmp/

# Playwright MCP scratch output (snapshots, screenshots) and dev-only demo
# screenshots for the permission-request progress view.
**/.playwright-mcp/
**/permission-*.png

# Dev-only visual mockup for the permission-request progress view (not part of
# the app or its tests)
system/apps/system_interface/frontend/permission-demo.html
system/apps/system_interface/frontend/src/permission-demo.ts

# A skill script run from its own directory must never leave workspace data beside
# its code; state belongs under the repo-root data/ tree only.
.agents/skills/*/scripts/data/
